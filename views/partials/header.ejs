<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Meeting Room Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
    <style>
      :root {
        --brand-primary: #6366f1; /* indigo */
        --brand-accent: #22d3ee;  /* cyan */
        --brand-success: #10b981; /* emerald */
        --brand-danger:  #ef4444; /* red */
        --surface: #ffffff;
        --muted: #6b7280;
        --ring: rgba(99, 102, 241, .35);
      }

      body {
        min-height: 100vh;
        background:
          radial-gradient(1200px 600px at 10% 10%, #f8fafc 0%, #eef2f7 40%, #e9eef5 100%);
        position: relative;
      }
      /* subtle decorative orbs */
      body::before, body::after {
        content: "";
        position: fixed;
        width: 420px; height: 420px;
        border-radius: 50%;
        filter: blur(60px);
        z-index: -1;
      }
      body::before { background: rgba(99,102,241,.10); top: -120px; right: -120px; }
      body::after  { background: rgba(34,211,238,.10); bottom: -120px; left: -120px; }

      .brand-logo {
        width: 36px; height: 36px;
        display: inline-grid; place-items: center; border-radius: .6rem;
        background: linear-gradient(135deg, var(--brand-primary), var(--brand-accent));
        color: #fff; font-weight: 700; letter-spacing: .5px;
        box-shadow: 0 6px 18px rgba(99, 102, 241, .35);
      }

      /* cards */
      .card { border-radius: .9rem; }
      .card.shadow-sm:hover { box-shadow: 0 .5rem 1.25rem rgba(15, 23, 42, .08) !important; }
      .card-header { border-bottom: 1px solid rgba(15,23,42,.06); }

      /* tables */
      .table thead th { font-weight: 600; color: #334155; }
      .table-hover tbody tr:hover { background-color: rgba(99,102,241,.04); }
      .badge.text-bg-primary { background: linear-gradient(135deg, var(--brand-primary), var(--brand-accent)); }
      .table td, .table th { vertical-align: middle; }
      /* keep action buttons on one line when possible */
      .actions { white-space: nowrap; }
      .actions .btn { white-space: nowrap; }

      /* buttons */
      .btn-primary {
        background: linear-gradient(135deg, var(--brand-primary), var(--brand-accent));
        border: none;
      }
      .btn-primary:hover { filter: brightness(1.05); }
      .btn-success { background: var(--brand-success); border: none; }
      .btn-danger { background: var(--brand-danger); border: none; }
      .btn-outline-primary { border-color: var(--brand-primary); color: var(--brand-primary); }
      .btn-outline-primary:hover { background: var(--brand-primary); color: #fff; }

      /* forms */
      .form-control:focus, .form-select:focus, .form-check-input:focus, .btn:focus {
        box-shadow: 0 0 0 .2rem var(--ring);
        border-color: rgba(99,102,241,.45);
      }
      .input-icon { position: relative; }
      .input-icon > i { position: absolute; left: .75rem; top: 50%; transform: translateY(-50%); color: var(--muted); }
      .input-icon > .form-control, .input-icon > .form-select { padding-left: 2.2rem; }

      /* helper */
      .muted { color: var(--muted); }
      /* avatar */
      .avatar-circle {
        width: 34px; height: 34px; border-radius: 50%;
        display: inline-grid; place-items: center;
        background: linear-gradient(135deg, var(--brand-primary), var(--brand-accent));
        color: #fff; font-weight: 700; letter-spacing: .5px;
      }
    </style>
  </head>
  <body class="bg-body-tertiary d-flex flex-column min-vh-100">
    <header class="sticky-top">
      <nav class="navbar navbar-expand-lg bg-white border-bottom shadow-sm">
        <div class="container">
          <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <span class="brand-logo">MR</span>
            <span class="fw-semibold">Meeting Room</span>
          </a>
          <% if (!locals.email) { %>
          <div class="ms-auto d-flex gap-2">
            <a href="/login" class="btn btn-outline-primary">Login</a>
            <a href="/signup" class="btn btn-primary">Sign up</a>
          </div>
          <% } else { %>
            <div class="dropdown">
              <button class="btn btn-outline-primary dropdown-toggle d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" aria-expanded="false" title="<%= locals.email %>">
                <span class="avatar-circle"><%= ((locals.email || 'U').split('@')[0][0] || 'U').toUpperCase() %></span>
              </button>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/api/auth/logout">Logout</a></li>
              </ul>
            </div>
          <% } %>
        </div>
      </nav>
    </header>
